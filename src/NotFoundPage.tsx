import { Link } from "react-router-dom"

export default function NotFoundPage() {
    return (
        <div>
            404 not found

        <p><Link to="/">Home from Link</Link></p>
        </div>
    )
}